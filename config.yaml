# Flux vidéo de votre caméra (Ezviz, Reolink, etc.)
camera_url: "rtsp://admin:VOTRE_CODE@192.168.1.X:554/live"

# Liste des plaques autorisées
authorized_plates:
  - "AB123CD"
  - "GE789MI"

# Système d'ouverture ("MQTT" ou "WEBHOOK")
output_method: "MQTT"

# Paramètres MQTT
mqtt_broker: "192.168.1.XX"
mqtt_topic: "gate/control"
mqtt_payload: "OPEN"

# Paramètres Webhook (si utilisé)
webhook_url: "http://IP_DE_VOTRE_RELAIS/open"
